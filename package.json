{
  "name": "quiz-meteor",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "deploy": "meteor deploy quizapp.meteorapp.com"
  },
  "dependencies": {
    "@babel/runtime": "^7.14.0",
    "node-fetch": "^2.6.1"
  }
}
